[project]
name = "cobalt-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ph-nixx", email = "pmiller0706@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
cobalt-backend = "cobalt_backend:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
